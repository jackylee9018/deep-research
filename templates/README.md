Place a branded `default.pptx` template in this directory to override the built-in fallback.

The PPT skill can generate a basic deck without this file, but production deployments should provide
a template whose layouts match `skills/pptx/layout_catalog.json`.
